# Text Granularity Extension

This directory is reserved for derived machine-readable artifacts for the IIIF
Text Granularity extension:

https://iiif.io/api/extension/text-granularity/

The extension is prose-only today for most implementation purposes. Any schema
or OpenAPI artifact added here must clearly state that it is derived rather
than upstream-canonical.
