# scripts

Helper scripts for artifact generation live here.

The initial scaffold only includes `tools/iiifgen` for vendoring upstream IIIF
artifacts. OpenAPI normalization/generation scripts will be added here as the
repo grows.
